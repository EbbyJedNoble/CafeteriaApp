import { View, Text, ScrollView } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function ConcessionaireScreen() {
	return (
		<SafeAreaView>
			<ScrollView>
				<Text>ConcessionaireHomeScreen</Text>
			</ScrollView>
		</SafeAreaView>
	)
}
